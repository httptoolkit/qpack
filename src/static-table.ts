import type { HeaderField } from './types.js';

/**
 * The QPACK static table, as defined in RFC 9204 Appendix A.
 */
export const STATIC_TABLE: readonly HeaderField[] = [
    // Filled in with all 99 entries when static table support is implemented
];
